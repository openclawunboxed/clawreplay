[project]
name = "clawreplay"
version = "1.3.1"
description = "replay checks for openclaw-style agent workflows before updates, model swaps, skill edits, and channel changes"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
